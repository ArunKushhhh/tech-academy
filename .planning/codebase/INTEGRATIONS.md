# Integrations

Currently, the application appears to be purely frontend-focused.

## External Services & APIs
- No external services or APIs are explicitly indicated in `package.json` or core code.
- Uses `shadcn/ui` components for styling and structure.

## Databases & Auth
- No database ORM or driver dependencies found.
- No authentication providers (like Auth.js, NextAuth, Firebase, Clerk, Supabase, etc.) found in the standard dependencies.
